<div class="container mt-5">
  <div class="row">
    <div class="col-md-10 offset-md-1 col-lg-8 offset-lg-2">
      <h3><%= t('.title') %></h3>
      <h5 class="my-4 ms-3"><%= "#{@user.active_relationships.count}フォロー" %></h5>
      <%= render partial: 'relationships/follow_user', collection: @users %>
      <div class="d-flex justify-content-center mt-4">
        <%= paginate @users %>
      </div>
    </div>
  </div>
</div>